# ========================================
# FLUENT - Environment Variables Setup
# ========================================

# ----------------------------------------
# SERVER-SIDE ONLY (Used in API Routes)
# ----------------------------------------
# These variables are NOT exposed to the browser
# Used by: /api/fund-wallet route

# Alchemy API Key for server-side RPC calls (OPTIONAL - if not set, will use NEXT_PUBLIC_ALCHEMY_RPC_URL)
ALCHEMY_API_KEY=your_alchemy_api_key_here

# Private key of your pre-funded wallet (fund this once from faucet)
# This wallet will auto-fund new users with 0.1 ETH
# Generate with: openssl rand -hex 32
# Then prefix with 0x and fund from: https://sepoliafaucet.com/
FUNDING_WALLET_PRIVATE_KEY=0x...

# ----------------------------------------
# CLIENT-SIDE (Exposed to Browser)
# ----------------------------------------
# These variables are accessible in client-side code
# Must be prefixed with NEXT_PUBLIC_

# Alchemy API Key (can be same as ALCHEMY_API_KEY above)
NEXT_PUBLIC_ALCHEMY_KEY=your_alchemy_api_key_here

# Full Alchemy RPC URL for Sepolia
NEXT_PUBLIC_ALCHEMY_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/your_alchemy_api_key_here

# Pimlico API Key for gasless transactions (bundler + paymaster)
NEXT_PUBLIC_PIMLICO_API_KEY=your_pimlico_api_key_here

# Web3Auth Client ID for Google OAuth login
NEXT_PUBLIC_WEB3AUTH_CLIENT_ID=your_web3auth_client_id_here

# ----------------------------------------
# HOW TO GET THESE API KEYS
# ----------------------------------------

# 1. ALCHEMY_API_KEY:
#    - Sign up at https://alchemy.com
#    - Create a new app on Sepolia testnet
#    - Copy the API key from dashboard

# 2. FUNDING_WALLET_PRIVATE_KEY:
#    - Generate: openssl rand -hex 32
#    - Prefix with 0x
#    - Get address from the private key
#    - Fund it with testnet ETH from https://sepoliafaucet.com/
#    - KEEP THIS SECRET - never commit to git!

# 3. PIMLICO_API_KEY:
#    - Sign up at https://dashboard.pimlico.io
#    - Create a new project
#    - Copy the API key

# 4. WEB3AUTH_CLIENT_ID:
#    - Sign up at https://dashboard.web3auth.io
#    - Create a new project
#    - Copy the Client ID

# ----------------------------------------
# NOTES
# ----------------------------------------

# • Copy this file to .env.local and fill in your actual values
# • .env.local is gitignored - never commit it!
# • ALCHEMY_API_KEY and NEXT_PUBLIC_ALCHEMY_KEY can be the same value
# • Server-side variables (without NEXT_PUBLIC_) are more secure
# • The funding wallet needs at least 1 ETH on Sepolia to fund ~10 users

